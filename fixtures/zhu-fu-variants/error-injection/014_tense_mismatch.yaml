injected:
  - entityId: "E4"
    attribute: "narrationTime"
    expectedValidator: "timeline"
    expectedSeverity: "warning"
    description: "E4 sceneType is flashback but narrationTime removed — timeline validator detects missing temporal context"
