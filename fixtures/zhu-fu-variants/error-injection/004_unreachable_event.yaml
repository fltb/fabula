injected:
  - entityId: "E5"
    attribute: "preconditions"
    expectedValidator: "reachability"
    expectedSeverity: "error"
    description: "E5 has a precondition that xianglins_wife.attribute has_magical_power = true — but no event ever sets this; the event is unreachable"
