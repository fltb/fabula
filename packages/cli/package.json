{
  "name": "@novalistically/cli",
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "nova": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --emitDeclarationOnly && node build.mjs",
    "build:js": "node build.mjs",
    "build:types": "tsc --emitDeclarationOnly",
    "test": "vitest run"
  },
  "dependencies": {
    "@novalistically/core": "*",
    "@novalistically/node-host": "*",
    "@novalistically/workbench-protocol": "*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
