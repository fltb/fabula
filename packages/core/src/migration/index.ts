// ============================================================================
// Novalistically — Migration (barrel)
// ============================================================================

export {
  CURRENT_SCHEMA_VERSION,
  type MigrationFn,
  migrateToLatest,
  migrations,
} from './registry.js';
