injected:
  - entityId: "system:genesis"
    attribute: "event"
    expectedValidator: "reachability"
    expectedSeverity: "warning"
    description: "System genesis event missing — no initial world facts (xianglins_wife.marital_status = widow_of_xianglin, location = weijia_shan, etc.) available, so E1/E2 preconditions referencing genesis facts are all unsatisfied."
  - entityId: "E1"
    attribute: "preconditions"
    expectedValidator: "reachability"
    expectedSeverity: "warning"
    description: "E1 precondition xianglins_wife.marital_status = 'widow_of_xianglin' depends on genesis-world fact that no longer exists — event is unreachable."
  - entityId: "E2"
    attribute: "preconditions"
    expectedValidator: "reachability"
    expectedSeverity: "warning"
    description: "E2 precondition xianglins_wife.location = 'weijia_shan' depends on genesis-world fact that no longer exists — event is unreachable."
