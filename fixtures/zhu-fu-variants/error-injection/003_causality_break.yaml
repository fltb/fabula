injected:
  - entityId: "E3"
    attribute: "preconditions"
    expectedValidator: "causality"
    expectedSeverity: "error"
    description: "E3 receives a deterministic location precondition whose value has no provider in the canonical story boundary."
