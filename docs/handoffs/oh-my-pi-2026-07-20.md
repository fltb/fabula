# oh-my-pi Implementation Handoff

> **时间**: 2026-07-20 11:43 CST
## Scope

The next worker owns implementation of the incomplete capabilities in `docs/TODO.md`. That document is the normative contract. This handoff intentionally contains no implementation change.

## Start Here

1. Start from the repository root so `.omp/AGENTS.md`, `.omp/RULES.md`, and `.omp/config.yml` load.
2. Verify the active configuration with `omp config list` and confirm the native context is present in the session context.
3. Read the relevant TODO capability section before touching code. Use `/plan` for each bounded cross-domain slice and obtain approval before implementation.
4. Use `/handoff` after each independently reviewable slice. Include changed paths, verification evidence, unresolved risks, and the exact next TODO capability.

## Contract Sources

- `docs/TODO.md`: current implementation requirements, capability gates, and S/C/X declarations.
- `AGENTS.md`: monorepo commands, architecture, fixtures, and repository constraints.
- `.slim/deepwork/progress.md`: historical bench-rewrite status; do not treat it as authorization to bypass `docs/TODO.md`.
- `docs/bench-rewrite-full.md` and `docs/bench-rewrite-design.md`: prior bench-rewrite design references.

## Execution Boundary

- Work one capability slice at a time. Do not mix state, graph, discourse, surface, corpus, or API changes unless the TODO dependency requires the same atomic contract.
- Preserve the logical/surface split: generated prose and Pass 2 validation never become WorldState, Knowledge, planned DiscourseState, graph providers, or causal dependencies.
- Every claimed deterministic capability needs the evidence required by its `CapabilityManifest` row. C capabilities require calibrated measurement evidence; X capabilities require typed rejection.
- Use existing storage abstractions in core modules; do not add direct `fs` access there.

## Dependency-Ordered Phase Map

Use these as handoff boundaries, not as one implementation batch:

1. **Foundation**: CLI-1 YAML ESM bundle, CLI-2/DAG-0 fixture failure, error hierarchy, structured logging, then tracing.
2. **Replay engine**: DAG-1 through DAG-5 and API-1 through API-5, ending with unified branch-compatible Story/Discourse replay boundaries and snapshots.
3. **Deterministic state and graph**: STATE-1, STATE-3, STATE-2, INTEGRATION-2, STATE-4, STATE-5, STATE-6, then GRAPH-1.
4. **Reader and render contracts**: DISCOURSE-1, RENDER-SURFACE-1, CAPABILITY-1, YAML-CONTRACT, AGG-1, and the logical/surface summarizers.
5. **Cross-domain corpus work**: INTEGRATION-1, CORPUS-1 through CORPUS-5, then Phase 2 metrics and performance/coverage gates.
6. **Operational extensions**: config hierarchy, migrations, reports, evidence verification, project style profile, plugins, interaction gates, agents, and API cleanup/docs.

Do not begin a later phase until its preceding phase has its required focused evidence. `docs/TODO.md` remains authoritative if this summary differs from an acceptance criterion there.

## First Manual Planning Prompt

Paste this into an interactive oh-my-pi session after verifying its native context:

```text
/plan Read @docs/handoffs/oh-my-pi-2026-07-20.md and the CLI-1 section of @docs/TODO.md. Scope this plan to CLI-1 only: repair the CLI YAML ESM bundle boundary. Do not edit files or run mutating commands. Establish the current reproduction, identify the exact package files and focused test/smoke command, define acceptance criteria and regression tests, and list explicit non-goals. Do not plan DAG, fixture, logging, or other TODO work in this slice.
```

After approving and completing that slice, hand off with its changed paths, focused evidence, remaining risks, and the next candidate: CLI-2/DAG-0.

## Verification

Use the smallest meaningful command first:

```bash
npx vitest run packages/core/tests/<relevant-suite>
npm run typecheck
npm run build
```

Run the full suite only when the changed contract crosses shared pipeline behavior or the TODO acceptance criteria explicitly require it. Exclude LLM E2E tests unless the local proxy and credentials are intentionally available.

## Working Tree At Handoff

- This handoff adds `.omp/AGENTS.md`, `.omp/RULES.md`, `.omp/config.yml`, and this document.
- `docs/TODO.md` is currently untracked and contains the completed requirements-specification work.
- `docs/INDEX.md` and `docs/reference/competitive-analysis.md` had unrelated pre-existing changes; do not revert or absorb them.
