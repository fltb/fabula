# AI Provider — Novalistically
# Set NOVALISTICALLY_AI_API_KEY. Base URL and model are optional.
#
# Defaults:
#   base URL: https://opencode.ai/zen/v1  (opencode zen, free tier)
#   model:    deepseek-v4-flash-free
#
# Override with NOVALISTICALLY_AI_BASE_URL and NOVALISTICALLY_AI_MODEL.
NOVALISTICALLY_AI_API_KEY=
# NOVALISTICALLY_AI_BASE_URL=
# NOVALISTICALLY_AI_MODEL=
