// ============================================================================
// Novalistically — Migration (barrel)
// ============================================================================

export {
  migrateToLatest,
  CURRENT_SCHEMA_VERSION,
  type MigrationFn,
  migrations,
} from './registry.js';
