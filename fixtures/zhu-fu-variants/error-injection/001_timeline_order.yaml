injected:
  - entityId: "E3"
    attribute: "narrationTime"
    expectedValidator: "timeline"
    expectedSeverity: "warning"
    description: "E3 sceneType is flashback but narrationTime is missing — timeline validator should detect missing temporal context"
