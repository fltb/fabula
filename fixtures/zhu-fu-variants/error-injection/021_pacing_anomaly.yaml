injected:
  - entityId: "E0"
    attribute: "arcPosition"
    expectedValidator: "pacing"
    expectedSeverity: "warning"
    description: "All 7 events have arcPosition: 'climax' — no narrative arc structure, pacing is broken"
